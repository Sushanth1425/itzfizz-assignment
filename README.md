Built using React + GSAP ScrollTrigger with scroll-synced animation.
Focused on smooth performance using transform-based animations and minimal layout thrashing.

Handled deployment-specific issues (GitHub Pages asset loading & scroll calibration) to ensure consistent behavior across environments.

Implemented dynamic scroll-driven letter reveal and staggered UI elements.
